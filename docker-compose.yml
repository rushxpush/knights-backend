services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    command: npm run start:dev
    ports:
      - '3001:3001'
  mongo:
    image: mongo
  test:
    build:
      context: .
      dockerfile: Dockerfile
    command: npm run test
    profiles:
      - test